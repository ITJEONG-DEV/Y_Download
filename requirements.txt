# 다운로드 엔진 (유튜브 정보 조회 + 다운로드)
yt-dlp>=2024.1.1

# 데스크톱 GUI — Essentials만 사용(QtWidgets/QtCore/QtGui). 'PySide6'(전체)는 QtWebEngine 등
# 대형 Addons까지 설치·번들하므로, 앱이 쓰는 Essentials만 받아 설치/배포 용량을 줄인다.
PySide6-Essentials>=6.6

# 썸네일 이미지 표시
Pillow>=10.0.0

# 썸네일 이미지 다운로드
requests>=2.31.0

# --- 개발/배포용 (실행에는 불필요) ---
# pyinstaller>=6.0    # exe 빌드 시 사용
